/** Home preview — keep the first viewport light after catalog growth */
export const HOME_RESTAURANT_PREVIEW = 6;
export const HOME_OFFER_PREVIEW = 4;

/** List pages — load more as the user scrolls */
export const LIST_PAGE_SIZE = 12;
